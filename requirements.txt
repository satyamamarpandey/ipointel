fastapi==0.141.1
uvicorn[standard]==0.52.4
SQLAlchemy==2.0.52
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
email-validator==2.3.0
psycopg[binary]==3.3.5
alembic==1.19.1
openpyxl==3.1.5
beautifulsoup4==4.15.0
python-multipart==0.0.32
pytest==9.1.1
starlette==1.6.0
google-auth==2.57.1
PyJWT==2.13.0
ruff==0.16.6
# Optional error reporting - inert unless SENTRY_DSN is set (see app/main.py).
# jinja2 is not used by this app directly: sentry-sdk auto-enables the
# Starlette integration, which imports it, and without it Sentry silently
# fails to start even with a valid DSN.
sentry-sdk==2.68.1
jinja2==3.1.6
